<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- GPS tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Requested separately at runtime, after fine location, so tracking survives backgrounding -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service for tracking through screen-lock / backgrounding -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Foreground-service notification (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Optional: only requested if the user turns on automatic trip detection.
         Lets Play Services tell us when a drive starts, using low-power sensors
         instead of us polling GPS in the background. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Route map tiles (OSMDroid / OpenStreetMap). No accelerometer permission needed. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <!-- Backup is off: EncryptedSharedPreferences restored onto a different device
         can't be decrypted and would crash the app at launch. -->
    <application
        android:name=".RevlineApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Revline">

        <!-- Sentry crash reporting. DSN is injected at build time (-PrevlineSentryDsn=...);
             an empty value disables the SDK entirely. -->
        <meta-data android:name="io.sentry.dsn" android:value="${sentryDsn}" />
        <meta-data android:name="io.sentry.environment" android:value="${sentryEnv}" />
        <meta-data android:name="io.sentry.anr.enable" android:value="true" />
        <meta-data android:name="io.sentry.traces.sample-rate" android:value="0.0" />

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Revline">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".TrackingActivity"
            android:exported="false" />

        <activity
            android:name=".TripSummaryActivity"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".LeaderboardActivity"
            android:exported="false" />

        <activity
            android:name=".ui.AdminDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".ui.admin.AnnouncementsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.NotificationsActivity"
            android:exported="false" />

        <activity
            android:name=".SearchActivity"
            android:exported="false" />

        <activity
            android:name=".UserListActivity"
            android:exported="false" />

        <activity
            android:name=".UserProfileActivity"
            android:exported="false" />

        <activity
            android:name=".CommentsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Auto-detect: receives IN_VEHICLE transitions from Play Services. -->
        <receiver
            android:name=".service.ActivityTransitionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.revline.tracker.action.ACTIVITY_TRANSITION" />
            </intent-filter>
        </receiver>

        <!-- Serves generated share cards to whichever app the user shares into. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".service.TrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>

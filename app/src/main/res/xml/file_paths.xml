<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Generated share cards live in cache/shares and are exposed read-only,
         per-intent, via FileProvider. -->
    <cache-path
        name="shares"
        path="shares/" />
</paths>

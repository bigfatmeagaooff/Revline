<?xml version="1.0" encoding="utf-8"?>
<!-- Redline hairline border only — drawn as a foreground so it sits over a
     profile photo. The slip_high fill lives in bg_avatar (the background). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="2dp" />
    <solid android:color="@android:color/transparent" />
    <stroke android:width="1.5dp" android:color="@color/redline" />
</shape>

# Add project specific ProGuard rules here.
# Minification is disabled for v1 (see app/build.gradle.kts). When enabling for a
# signed release, validate Room entities and ViewBinding survive shrinking.

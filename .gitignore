*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/release
*.apk
*.keystore
*.jks
# ...except the shared debug key, which MUST be committed so every build signs
# the same way (see signingConfigs in app/build.gradle.kts).
!/app/revline-debug.keystore
